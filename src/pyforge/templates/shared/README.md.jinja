# {{ project_name }}

{{ description }}

## Overview

- Template: `{{ template }}`
- Author: `{{ author }}`
- License: `{{ license }}`

{% if template == 'fastapi' %}
- Entry point: `src/{{ project_name }}/main.py`
- Run the API with `uvicorn src.{{ project_name }}.main:app --reload`
{% elif template == 'flask' %}
- Entry point: `src/{{ project_name }}/app.py`
- Run the app with `flask run`
{% elif template == 'cli' %}
- Entry point: `src/{{ project_name }}/cli.py`
- Run the command after installation with `{{ project_name }}`
{% else %}
- Entry point: `src/{{ project_name }}/main.py`
- Run the package with `python -m {{ project_name }}`
{% endif %}

## Quick Start

1. Install the project dependencies:
   ```bash
   {% if use_justfile %}just install{% else %}make install{% endif %}
   ```
{% if use_pre_commit %}
2. Install pre-commit hooks:
   ```bash
   pre-commit install
   ```
{% endif %}
3. Run the test suite:
   ```bash
   {% if use_justfile %}just test{% else %}make test{% endif %}
   ```

## Development

Format code:
```bash
{% if use_justfile %}just format{% else %}make format{% endif %}
```

Run the application:
```bash
{% if use_justfile %}just run{% else %}make run{% endif %}
```
